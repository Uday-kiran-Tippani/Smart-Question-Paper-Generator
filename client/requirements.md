## Packages
recharts | For analytics dashboard charts
framer-motion | Smooth transitions and animations
lucide-react | Icons (already in base, but emphasizing usage)
react-hook-form | Form handling
zod | Schema validation
@hookform/resolvers | Zod resolver for react-hook-form

## Notes
The backend uses session-based auth, so `credentials: "include"` is required for all requests.
Paper generation requires sending distribution percentages for Easy/Medium/Hard.
